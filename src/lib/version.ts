// The current app version — kept in lockstep with the service-worker VERSION in public/sw.js.
// Shown small on the login screen so we can SEE which build a device is actually running
// (the recurring "is this the fresh code or the stale cache?" question).
export const APP_VERSION = "cn-v467";
